<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.sanjay.kangaroorooms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/FirstRumTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label=" "
            android:theme="@style/FirstRumTheme" />
        <activity
            android:name=".SignupActivity"
            android:label=" "
            android:theme="@style/FirstRumTheme" />
        <activity
            android:name=".VerifyPhone"
            android:label=" "
            android:theme="@style/FirstRumTheme" />
        <activity
            android:name=".FirstActivity"
            android:label=" "
            android:theme="@style/FirstRumTheme" />
        <activity
            android:name=".CouponsActivity"
            android:theme="@style/FirstRumTheme" />
        <activity
            android:name=".SearchActivity1"
            android:theme="@style/FirstRumTheme" />
        <activity android:name=".AllVehiclesActivity" />
        <activity android:name=".HotelsActivity"></activity>
    </application>

</manifest>